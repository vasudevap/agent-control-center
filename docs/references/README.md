# References

This directory preserves durable supporting material and provenance that is useful to implementation but is not itself an accepted architecture or product decision.

`claude-workspace/` contains historical operating and UI guidance from the source implementation workspace. Root `AGENTS.md` and canonical documentation take precedence.
