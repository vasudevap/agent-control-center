{
  "name": "atlas-agent-control-center",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace @atlas/web run dev",
    "build": "npm --workspace @atlas/web run build",
    "start": "npm --workspace @atlas/web run start",
    "lint": "npm --workspace @atlas/web run lint",
    "typecheck": "npm --workspace @atlas/web run typecheck"
  }
}
